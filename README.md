# autoclaude

Autonomous Claude Code session runner and Anthropic SDK CLI, built onion-style
on top of `claude-agent-sdk` and `anthropic`. See `docs/` (forthcoming) and the
approved design at `.claude/plans/` for the full architecture.

## Status

Milestone 1 (pure domain core) is implemented and tested. See `src/autoclaude/domain/`.
